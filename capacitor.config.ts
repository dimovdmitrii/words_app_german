import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dimovdmitrii.vokabeln',
  appName: 'Deutsch Vokabeln',
  webDir: 'dist'
};

export default config;
